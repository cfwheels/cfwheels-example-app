/**
 * Set configuration values
 *
 * {code:bash}
 * wheels set environment development
 * wheels set settings cacheQueries false
 * wheels set datasource myapp
 * {code}
 */
component extends="base" {
	// This is a namespace command - subcommands are in the set/ directory
	// CommandBox will automatically handle subcommand routing
}